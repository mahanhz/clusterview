spring:
  cache:
    jcache:
      config: ehcache.xml
  jpa:
    generate-ddl: true

server:
  error:
    whitelabel:
      enabled: false
  session:
    timeout: 600 # 10 minutes

management:
  context-path: /manage